#!/bin/sh

# Now executing the create-user command from the command-line, 
# a new record will be added to the users table.

myapi create-user --name 'test user' --email test_user@myapi.com --password 'password'